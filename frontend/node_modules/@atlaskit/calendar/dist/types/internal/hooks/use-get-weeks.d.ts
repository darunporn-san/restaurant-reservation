import type { WeekDay } from '../../types';
import type { Week } from '../types';
export default function useGetWeeks({ day, month, year, today, disabled, selected, previouslySelected, weekStartDay, }: {
    day: number;
    month: number;
    year: number;
    today: string;
    disabled: string[];
    selected: string[];
    previouslySelected: string[];
    weekStartDay: WeekDay;
}): Week[];
